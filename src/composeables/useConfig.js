export const useConfig = () => {
  const config = {
    apps: [
      {
        name: '声工坊',
        value: 'voice_studio',
        icon: 'graphic_eq',
        path: '/',
      },
    ],
  }
  return {
    config,
  }
}
